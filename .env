# === Nightscout Basic Config ===

# MongoDB connection string (Railway will generate this when you add the MongoDB plugin)
MONGO_CONNECTION=mongodb+srv://<username>:<password>@<cluster-url>/<dbname>?retryWrites=true&w=majority

# Secret password used to authenticate API access (min 12 chars, keep this private)
API_SECRET=yourStrongSecretHere123

# Units for blood glucose display: "mg/dl" or "mmol"
DISPLAY_UNITS=mmol

# Enable useful Nightscout features (you can add/remove modules later)
ENABLE=careportal basal pump iob cob

# Set timezone for correct reporting
TIMEZONE=Europe/Berlin

# === Optional Settings (safe defaults) ===

# Language for UI
LANGUAGE=de

# Theme: "default", "colorblindfriendly", etc.
THEME=default

# Allow public read access (set to "true" for school monitoring without login)
AUTH_DEFAULT_ROLES=denied
AUTH_DEFAULT_ROLES_READ=guest

# === Advanced (leave as is for now) ===
PORT=1337